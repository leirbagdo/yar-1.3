/**
 * assets/js/config.js
 *
 * Antes cada página HTML repetia a mesma linha:
 *   const API_URL = 'http://localhost:8080';
 * em pelo menos 6 arquivos diferentes. Centralizado aqui para que
 * uma mudança de ambiente (ex.: deploy em produção) seja feita em
 * um único lugar.
 */

const API_URL = 'http://localhost:8080';
